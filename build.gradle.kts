// File di build "radice": dichiara le versioni dei plugin usati dai moduli.
// I plugin veri vengono applicati nel modulo :app (vedi app/build.gradle.kts).
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
